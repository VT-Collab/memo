# From Local Corrections to Generalized Skills: Improving Neuro-Symbolic Policies with MEMO

To run the code, see `python main.py`. 
To prune the skillbook, see `python pruning.py`.

The prompts are in `config/prompts/`.

Various scenes are in `config/scenes/user_study`.

The tasks are in `config/tasks/tasks.yml`.
